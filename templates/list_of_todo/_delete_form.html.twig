<form method="post" action="{{ path('app_list_of_todo_delete', {'id': list_of_todo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ list_of_todo.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
