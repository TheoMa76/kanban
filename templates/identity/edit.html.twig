{% extends 'base.html.twig' %}

{% block title %}Edit Identity{% endblock %}

{% block body %}
    <h1>Edit Identity</h1>

    {{ include('identity/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_identity_index') }}">back to list</a>

    {{ include('identity/_delete_form.html.twig') }}
{% endblock %}
